"use strict";

/*
 * # Node.js
 *
 * - 运行在服务器端的 JavaScript
 * - 用来编写服务器
 * - 特点
 *   - 单线程
 *   - 异步
 *   - 非阻塞
 *   - 统一 API
 *
 * ## nvm
 *
 * - 命令
 *   - `nvm list`：显示已安装的 Node.js 版本
 *   - `nvm install 版本`：安装指定版本的 Node.js
 *   - `nvm use 版本`：使用指定版本的 Node.js
 */
console.log("Hello World!");
